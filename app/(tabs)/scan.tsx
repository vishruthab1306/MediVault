import React from 'react';
import { View } from 'react-native';

// This is just a placeholder screen. The scan button in the tab bar intercepts the press
// and navigates directly to the scan-flow stack.
export default function ScanScreen() {
  return <View />;
}
